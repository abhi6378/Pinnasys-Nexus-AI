# Project Rules

- Never change the orchestrator contract without updating all callers.
- Never treat session_state as the only source of truth.
- Persist chat, workflow runs, ideas, and memory consistently.
- Keep workflow execution and chat execution aligned.
- Do not add new features before Step 1 is stable.
- Do not make large refactors without first mapping the flow.